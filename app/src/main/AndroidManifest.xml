<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- أذونات لازمة -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE"/>

    <application
        android:allowBackup="true"
        android:label="Auto Math Tapper 2"
        android:icon="@android:drawable/sym_def_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AutoMathTapper2">

        <!-- خدمة الـForeground لالتقاط الشاشة -->
        <service
            android:name=".MediaProjectionFgService"
            android:exported="false" />

        <!-- الأنشطة -->
        <activity android:name=".SymbolUploaderActivity" android:exported="false" />
        <activity android:name=".SymbolTestActivity" android:exported="false" />
        <activity android:name=".PickRoiActivity" android:exported="false" />

        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- خدمة الـAccessibility للضغط التلقائي -->
        <service
            android:name=".AutoTapService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
    </application>
</manifest>
